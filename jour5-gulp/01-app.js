// node-app.js option1 option2

console.log(process.argv)

// créer un script qui permet de réaliser les tâches pour vous 
// gulp => https://gulpjs.com/

// npm i -g gulp
// gulp => créer un fichier =>  gulpfile.js

// gulp => gulpfile.js exécuter 

// gulp.task() => permet de créer une action que l'on pourra appeler en ligne de commande
// gulp.task("tache 1" , () => {  })

// gulp.src() : définir le ou les fichiers à manipuler 

// gulp.src("style.css")

// gulp.dest() // une fois que la manipulation sont réalisées => copie dans le dossier de destination 
// gulp.dest("/prod")

// https://www.npmjs.com/package/gulp-autoprefixer

// permet d'ajouter tous les prefix pour des propriétés css 

